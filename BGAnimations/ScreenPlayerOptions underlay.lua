return Def.ActorFrame {
	OnCommand = function(self)
		-- Holding off on this until it's more stable and I'm more well-versed in manipulating Def.NoteField. ~Sudo
		--self:AddChildFromPath(THEME:GetPathG('Players', 'preview'))
	end
}
